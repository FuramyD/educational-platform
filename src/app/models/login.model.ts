export interface LoginParameters {
    login: string;
    password: string;
    rememberMe: boolean;
}

export interface RegistrationParameters {
    userName: string;
    email: string;
    firstName: string;
    lastName: string;
    password: string;
}
